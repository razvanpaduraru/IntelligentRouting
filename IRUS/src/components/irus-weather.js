import { LitElement, html, css } from "lit-element";

class IrusWeather extends LitElement {
  constructor() {
    super();
  }

  render() {
    return html``;
  }
}

window.customElements.define("irus-weather", IrusWeather);
